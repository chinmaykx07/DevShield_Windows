// main_stub.go is intentionally left empty for non-Windows builds.
// The real entrypoint is in main.go and the platform-specific single-instance
// logic is split across instance_windows.go and instance_other.go.

package main
